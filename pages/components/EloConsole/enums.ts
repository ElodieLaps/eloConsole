export enum ConsoleActionTypeEnum {
   toggle = "toggle",
   open = "open",
   close = "close",
   write = "write",
   addLine = "addLine",
   clear = "clear",
   reset = "reset"
}

export enum CommandLineEnum {
   help = 'help',
   educ = 'educ',
   exp = 'exp',
   info = 'info'
}